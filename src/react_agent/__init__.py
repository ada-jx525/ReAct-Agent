"""Customer-support agent package.

Import the compiled graph explicitly from ``react_agent.graph``. Importing data
utilities must not load PyTorch, construct a graph, or initialize chat clients.
"""
